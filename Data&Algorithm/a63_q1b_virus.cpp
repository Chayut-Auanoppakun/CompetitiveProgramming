#include<bits/stdc++.h>
using namespace std;

bool find_virus(vector<int> &v,int k)
{
    vector<int> a,b;
    for(int )
}

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(nullptr);
    int n,k;
    cin>>n>>k;
    vector<int> virus((2^k)+1);
    for(int i=0;i<n;++i)
    {
        for(int j=0;j<2^k;++k)
        {
            cin>>virus[j];
        }
    }
}